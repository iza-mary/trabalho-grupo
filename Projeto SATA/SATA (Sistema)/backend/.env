# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=admin123
DB_NAME=sistema_idosos
DB_PORT=3306
DB_POOL_SIZE=10

JWT_SECRET=supersecret_jwt_change_me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@12345

# SMTP configuration for email sending (NodeMailer)
# Host of the SMTP server (Gmail)
SMTP_HOST=smtp.gmail.com
# Port 587 for STARTTLS (secure=false) / 465 for SSL (secure=true)
SMTP_PORT=587
# Authentication user (full email address)
SMTP_USER=satasyst3m@gmail.com
# Authentication password or App Password (DO NOT COMMIT REAL SECRETS)
SMTP_PASS=xdjg ouny zcad mxbk
# Whether to use SSL directly (true for port 465, false for 587 STARTTLS)
SMTP_SECURE=false
# Enforce TLS upgrade on connection (recommended for 587)
SMTP_REQUIRE_TLS=true
# Reject self-signed certificates (keep true in production)
SMTP_TLS_REJECT_UNAUTHORIZED=true
# Default FROM address used in emails
SMTP_FROM=satasyst3m@gmail.com
# Display name for FROM address
SMTP_FROM_NAME=SATA – Sistema para Automatização de Tarefas do Asilo

# Frontend base URL used in links (validation/reset flows)
FRONTEND_URL=http://localhost:5173

# Email token TTLs (optional; defaults: 24h validation, 15min reset)
EMAIL_VALIDATION_TTL_HOURS=24
RESET_TOKEN_TTL_MINUTES=15